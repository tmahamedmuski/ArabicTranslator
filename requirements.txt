# Core dependencies
torch>=2.0.0
transformers>=4.35.0
sounddevice>=0.4.6
numpy>=1.24.0

# Optional but recommended
accelerate>=0.24.0
safetensors>=0.4.0

# Audio processing (choose one based on your needs)
webrtcvad>=2.0.10  # For WebRTC VAD (optional, more accurate speech detection)

# Development/debugging
scipy>=1.11.0  # For audio file operations